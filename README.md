# Marloth Strategy

A single-player turn-based strategy game set in the Marloth setting.

Currently a C# ASCII console prototype (.NET 8).

```bash
dotnet run --project src/MarlothStrategy.Console.App
```
